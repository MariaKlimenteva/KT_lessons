Сообщения
Порция данных которые один процесс передает другому
ftok() - генерируем ключ, на вход файл
по ключу создаем очередь сообщений: msgget()
Атомарные операции: send, receive

msgsnd
// 4-ый арг - флаги, можно null
// Передать сообщение: передается в формате структуры, сами объявляем ее, 
// ограничения на создаваемую структуру: должна начинаться с переменной типа long,
// в которую мы сораним тип сообщения
// struct my_struct{
//     long type; (тип должен быть больше нуля)
//     int a;
//     long b;
//     float c[10];
// }

// далее создаем переменную типа данной структуры. передаем эту переменную в качестве 2 арг 
// 3 арг - размер sizeof переменной
// 4 арг флаги 0

// msgrcv - 1 вызов читает 1 сообщение в порядке очереди
// объявление структуры в header, который можем подключить в обоим программам

// 3 аргумент надо передавать sizeof структуры минус sizeof(long)

// 4 аргумент - позволяет фильтровать получаемые сообщения
// тип сообщения: в зависимости от типа сообщения сортирует сообщения
// алгоритмы работы msgrcv:
// 1) чтение всех сообщений вне зависимости от типа - 4ый аргумент = 0
// 2) 4ый аргумент положительное число - то в порядке очереди считывает в из очереди сообщение с типом который передали
// 3) 4ый аргумент - отрицательное считывает все сообщения меньше модуля этого значения, считывая по порядку 

// 5ый аргумент флаги -> пишем там 0
